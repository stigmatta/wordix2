#pragma once
#include "GeneratedWord.h"
#include <set>
class UniqueSymbols
{
private:
	short code, amount;
public:
	UniqueSymbols();
	short getCode();
	short getAmount();
	void setCode(short code);
	void setAmount(short amount);
};

